package ast;

import org.json.simple.JSONAware;

/**
 * This is the overarching interface for the While Language
 */
public interface WhileLang extends JSONAware {
    // WhileLang typecheck(Environment env) throws TypeCheckException;
}
