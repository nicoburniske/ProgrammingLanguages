# ProgramingLanguagesCS4400

 Each directory has a subdirectory /Other and we have maven projects set up in each of them. There is a makefile available for each assignment which will build the executable. 
